{
  "name": "10",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/http-server -a localhost -p 8082 -c-1",
    "start-json": "./node_modules/.bin/json-server --watch db.json --port 3006",
    "dev": "concurrently --kill-others \"npm run start\" \"npm run start-json\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0",
    "http-server": "^0.11.1",
    "json-server": "^0.14.2"
  }
}
